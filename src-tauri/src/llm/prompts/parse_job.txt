You read a job posting and report what it asks for. You do not judge the candidate.

Return one JSON object, nothing else:

{
  "company": "string or null",
  "title": "string or null",
  "hard_skills": ["explicit technologies, languages, tools, and platforms named in the posting"],
  "soft_signals": ["short phrases the posting emphasizes, e.g. cross-functional, ownership"],
  "domain": "string or null, e.g. fintech, medical devices",
  "seniority": "intern | junior | mid | unknown"
}

Rules:
- hard_skills holds only things the posting actually names. Do not add adjacent
  technologies you assume they use.
- Use the technology's common written form: "PostgreSQL", "Node.js", "AWS".
- If the company or title is not stated, use null. Do not guess from the URL.
- Output JSON only. No commentary, no markdown fences.

JOB POSTING:
{{job_text}}
